# changes/

變更提案區(OpenSpec 慣例):每個變更 `changes/<change-id>/`,含 `proposal.md`(Why/What/Impact)、`tasks.md`(實作 checklist)、選配 `design.md`(跨系統/有取捨時)、`specs/<capability>/spec.md`(ADDED/MODIFIED/REMOVED delta)。實作完成 → delta sync 回 `../specs/`、資料夾移入 `archive/`。

已知待開提案(2026-08-26):
- kline 分割畫面(現股+轉債同屏)
- 左欄 hover 自動縮放的手動開合按鈕版本
- 中欄一鍵完全收合 + 窄寬 RWD 精修
